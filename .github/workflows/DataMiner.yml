name: gfl-data-miner auto update bot

on:
  workflow_dispatch:
    inputs:
      cmdargs:
        description: 'Commandline Arguments'     
        required: false
        default: 'tw kr jp us ch' 
        type: string
  schedule:
    - cron: '0 0/3 * * *'
    

jobs:
  gfl-data-miner-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.PERSONAL_TOKEN }}
          submodules: 'recursive'
      - name: git
        run: |
          git --version
          git config --global user.name "AutoUpdate"
          git config --global user.email "<>"
          git config --global core.autocrlf input
          git submodule foreach --recursive git checkout main
      - name: Get current time
        uses: josStorer/get-current-time@v2
        id: current-time
        with:
          format: YYYY-MM-DD
          utcOffset: "+08:00"

      - name: Setup Python
        uses: actions/setup-python@v2.2.2
        with:
          python-version: '3.8'
      - name: Install Python dependencies
        run: |
          pip install -r data-miner/requirements.txt
      - name: Data Miner
        run: python data-miner/data_miner.py ${{ inputs.cmdargs || 'tw kr jp us ch' }} 
      - name: CheckDifference
        id: difference
        run: |
          git add .
          git diff-index HEAD
          git diff-index --quiet HEAD || echo '::set-output name=diff::true'
      - name: Analysis Updates
        if: steps.difference.outputs.diff=='true'
        run: |
          pip install -r requirements.txt
          python enemy_team_analyze/enemy_team_analyze.py
          python enemy_stats_analyze/main.py
      - name: Resource Downloader Update
        if: steps.difference.outputs.diff=='true'
        run: |
          cd resource_downloader
          python pack_resdata.py
          git add .
          git diff-index --quiet HEAD || git commit -m "data-miner auto update ${{ steps.current-time.outputs.formattedTime }}"
          cd ..
      - name: Anti Harmonization Update
        if: steps.difference.outputs.diff=='true'
        run: |
          python anti-harmonization/main.py
          cp anti-harmonization/catchdata.dat outputs/catchdata.dat
      - name: Commit Changes
        if: steps.difference.outputs.diff=='true'
        run: |
          git add .
          git commit -m "data-miner auto update ${{ steps.current-time.outputs.formattedTime }}"
      - name: Push changes
        run: |
          git submodule foreach --recursive git push
          git push
